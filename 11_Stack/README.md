# Monotonic stack

## Monotonic Stack Template

``` py
stk = []
for i in range(len(A)):
    while stk and `condition`:
        `logic`
        stk.pop()
    if `condition`:
        stk.append(i)
return ans
```
