# Fixed Window

``` py
def fixed_window(A, k):
    sm = # init first k elements in the fixed window
    ans = # init answer
    for i in range(k, len(A)):
        # update sm
        # update ans
    return ans
```